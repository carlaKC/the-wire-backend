Extract factual claims from each document, assign each document to one reusable topic, and validate each claim against the source document. Return one strict JSON object with a documents array. When an existing topic fits, set topic.id to its integer id. When no existing topic fits, set topic.id to 0 and provide a concise topic.title and topic.description. All id fields that are numeric in the schema must be JSON numbers, never words or translated text.
